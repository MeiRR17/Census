version: '3.8'

services:
  # ===========================================================================
  # Telephony Management Application
  # ===========================================================================
  telephony-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telephony-management-app
    restart: unless-stopped
    ports:
      - "8080:8080"  # Application port
    environment:
      # API Gateway Configuration
      API_GATEWAY_URL: http://api-gateway:8001
      API_KEY: telephony-app-key-123
      
      # AXLerate Configuration (Core service)
      AXLERATE_BASE_URL: http://10.X.X.X:8002  # Core server IP
      AXLERATE_API_KEY: ${AXLERATE_API_KEY:-default-api-key}
      
      # Application Configuration
      APP_NAME: Telephony Management
      APP_VERSION: 1.0.0
      ENVIRONMENT: production
      TZ: Asia/Jerusalem
      
      # Database Configuration (if needed)
      DB_HOST: localhost
      DB_PORT: 5432
      DB_NAME: telephony_app
      DB_USER: telephony_user
      DB_PASSWORD: ${DB_PASSWORD:-secure_password}
      
      # Logging Configuration
      LOG_LEVEL: INFO
      LOG_FORMAT: json
      
    volumes:
      - app_data:/app/data
      - app_logs:/app/logs
      - ./config:/app/config:ro
    networks:
      - telephony-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    depends_on:
      - telephony-db

  # ===========================================================================
  # Local Database for Telephony App (if needed)
  # ===========================================================================
  telephony-db:
    image: postgres:15-alpine
    container_name: telephony-app-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: telephony_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secure_password}
      POSTGRES_DB: telephony_app
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
    volumes:
      - telephony_db_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - telephony-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U telephony_user"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

# =============================================================================
# Named Volumes for Data Persistence
# =============================================================================
volumes:
  app_data:
    driver: local
  app_logs:
    driver: local
  telephony_db_data:
    driver: local

# =============================================================================
# Docker Networks
# =============================================================================
networks:
  # Edge network - connects to API Gateway
  telephony-network:
    external: true
